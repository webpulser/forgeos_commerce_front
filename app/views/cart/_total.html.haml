.total_block
  .block
    .grid_4.alignright
      Sous total panier
    .grid_6.alpha.omega
      %span.space
        &nbsp;
      = price_with_currency(current_cart.total(true, false))
    .clear
    .clear
  .block
    .grid_4.alignright
      Réduction
    .grid_6.alpha.omega
      %span.space
        &nbsp;
      = price_with_currency(current_cart.special_offer_discount ? -current_cart.special_offer_discount : 0)
    .clear
  .block
    .grid_4.alignright
      Frais de port
    .grid_6.alpha.omega
      %span.space
        &nbsp;
      =# price_with_currency(current_cart.shipping_price)
    .clear
  %br
  #total_block.block
    .grid_4.alignright
      Prix total TTC
    .grid_6.alpha.omega
      %span.big_price
        #{price_with_currency(current_cart.total(true,true))} TTC
    .clear

.order_block
  .grid_4.alignright
    - link_to root_path do 
      Continuer vos achats
  .grid_6.alpha.omega
    - form_tag url_for(:controller => 'order', :action => 'new'), :method => :post do
      %span.space
        &nbsp;
      = submit_tag 'Commander'
  .clear
