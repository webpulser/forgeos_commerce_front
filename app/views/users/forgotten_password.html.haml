.account-area
  .prefix_4.grid_8
    %h2= t(:forgotten_password, :scope => [:user, :action]).capitalize
    - form_tag(reset_password_user_path, :html => { :autocomplete => false }) do
      .grid_5
        = label_tag(:email, t(:email))
        = text_field_tag(:email)
      .grid_5
        = submit_tag('send', :id => 'forgotten_password_submit')
      .clear
  .clear
